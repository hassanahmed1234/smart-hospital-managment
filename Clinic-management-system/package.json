{
  "name": "backend",
  "version": "1.0.0",
  "description": "ClinicOS Backend - JavaScript",
  "main": "api/server.js",
  "scripts": {
    "start": "node --watch backend/api/server.js",
    "dev": "node --watch backend/api/server.js",
    "seed": "node backend/api/seed.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.4.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "rimraf": "^6.1.3"
  }
}
