{
  "version": 2,
  "builds": [
    { "src": "backend/api/app.js", "use": "@vercel/node" },
    { "src": "frontend/package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/api/app.js" },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}